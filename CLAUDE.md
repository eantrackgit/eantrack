# EANTrack — Instruções para Claude Code / Codex

## PROIBIDO — Nunca executar

- `dart format` — trava a execução no ambiente atual
- `flutter format` — mesma razão
- `flutter analyze` — trava a execução no ambiente atual
- `flutter test` — trava a execução no ambiente atual
- Qualquer comando Flutter/Dart de terminal

## Validação de código

NÃO rodar nenhum comando de validação.
Apenas escrever o código correto e entregar.
O desenvolvedor valida manualmente.
