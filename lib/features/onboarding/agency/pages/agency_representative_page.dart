import 'package:flutter/material.dart';

class AgencyRepresentativePage extends StatelessWidget {
  const AgencyRepresentativePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Representante Legal'),
      ),
      body: const Center(
        child: Text('Em construção'),
      ),
    );
  }
}
